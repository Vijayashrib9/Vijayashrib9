-- query to convert 1 to 0, 0's to 1 in a col
--create table t1 as 

select * from ones_zeros

select NVL2(NULLIF(C0L1,0) , 0 , 1), C0L1, decode(C0L1,1,0,0,1) from ones_zeros

UPDATE ones_zeros SET  C0L1=decode(C0L1,1,0,0,1);

UPDATE ones_zeros SET C0L1 = NVL2(NULLIF(C0L1,0) , 0 , 1);
